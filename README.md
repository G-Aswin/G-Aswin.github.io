# Installation
- Install Ruby with ridk, gem, bundler
- Install jupyter within pip

# Run commands to locally test
- `bundle install`
- `bundle exec jekyll serve`

# Deployment
- `bin/deploy --user`

# Known issues, to-dos
- [ ] Google analytics are not working
- [ ] Cleanup unnecessary pages and files to reduce repo size
- [ ] Cleanup deployment stages and actions
- [ ] Populate the `projects` and `repositories` pages
- [ ] Detailed how-to guide to set up this webpage (medium)
